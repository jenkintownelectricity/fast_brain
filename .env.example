# ═══════════════════════════════════════════════════════════════════════════════
# Fast Brain Training Pipeline - Environment Variables
# Set these in Modal Secrets or pass via modal run --env
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# YOUR MODAL ENDPOINTS (Already deployed! Use these instead of paying for APIs!)
# ─────────────────────────────────────────────────────────────────────────────

# Premier Whisper STT - For audio/video transcription (95% cheaper than OpenAI!)
MODAL_WHISPER_URL=https://jenkintownelectricity--premier-whisper-stt-transcribe-web.modal.run

# Kokoro TTS - For voice synthesis testing
MODAL_KOKORO_URL=https://jenkintownelectricity--hive215-kokoro-tts-synthesize-web.modal.run
MODAL_KOKORO_VOICES_URL=https://jenkintownelectricity--hive215-kokoro-tts-list-voices.modal.run

# Coqui TTS - For voice cloning
MODAL_COQUI_URL=https://jenkintownelectricity--premier-coqui-tts-synthesize-web.modal.run
MODAL_COQUI_CLONE_URL=https://jenkintownelectricity--premier-coqui-tts-clone-voice-web.modal.run

# ─────────────────────────────────────────────────────────────────────────────
# LLM APIs (Still needed for extraction & scoring)
# ─────────────────────────────────────────────────────────────────────────────

# Groq - For fast LLM inference (extraction, scoring, vision OCR)
GROQ_API_KEY=your_groq_api_key

# ─────────────────────────────────────────────────────────────────────────────
# OPTIONAL FALLBACKS
# ─────────────────────────────────────────────────────────────────────────────

# OpenAI - Only needed if you want fallback when Modal is down
OPENAI_API_KEY=your_openai_api_key
USE_OPENAI_FALLBACK=false

# ─────────────────────────────────────────────────────────────────────────────
# DATABASE (Modal Volume - SQLite stored at /data/fast_brain.db)
# ─────────────────────────────────────────────────────────────────────────────

FAST_BRAIN_DB_PATH=/data/fast_brain.db
